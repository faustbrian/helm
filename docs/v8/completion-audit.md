# V8 Completion Audit

This is an evidence inventory, not a declaration of completion. `Implemented`
means the repository contains a production path and focused automated tests.
`Partial` means only part of the acceptance criterion is implemented. `Pending
live evidence` means repository behavior exists but the required real platform
or workload record has not been committed. No row with either pending state may
be treated as release acceptance.

Snapshot date: 2026-07-16. The local full-suite evidence at the snapshot was
`cargo test --quiet`: 884 passed, 0 failed, and 24 explicitly gated tests
ignored. Twenty-three are CI-owned live Engine acceptances; one is the
release-mode discovery performance benchmark. `just lint` (format plus
all-target Clippy with the repository's
configured severities), `just build`,
`scripts/audit-v8-host-dependencies.sh`,
`scripts/audit-v8-workflow-actions.sh`,
`scripts/audit-v8-supply-chain.sh`, and `git diff --check` also passed. Those
local commands do not substitute for the platform and benchmark artifacts
identified below.

[External verification](external-verification.md) assigns each remaining check
to local verification, GitHub Actions, a persistent macOS/Linux host, or a
controlled benchmark environment. Missing external inputs do not invalidate
the local gate, but their evidence remains mandatory for release acceptance.

## Product and configuration

| ID | Requirement | State | Authoritative evidence | Missing evidence or work |
|---|---|---|---|---|
| AC-01 | One login-started per-user daemon is authoritative | Partial | `src/control_plane/daemon/unix_daemon_runtime.rs`; one-command rollback-aware setup, canonical watched-root validation, lease-protected watched-root publication, symbolic-link-refusing private singleton lease with bounded owner-PID diagnostics, IPC-readiness-gated transactional login-service installation, crash-recoverable directory-locked definition publication, verified manager cleanup, manager-and-IPC-backed status and explicit readiness-gated restart, bounded symbolic-link-refusing private host logging, and Unix service-manager tests | Live macOS and Linux login/reboot records |
| AC-02 | Watched-root YAML addition activates without manual start | Implemented | `reconcile_watched_roots`; fail-closed pending-lock planning; typed Engine resolution; atomic no-clobber automatic lock publication; immediate rescan; discovery, debounce, complete-plan, automatic-lock, and daemon reconciliation tests | Live claimed-platform add/edit/remove record |
| AC-03 | TOML is absent from v8 config and state paths | Implemented | strict YAML loader, unsupported-TOML discovery diagnostic, YAML lock tests, and removal of executable legacy parity harnesses | Clean-install acceptance on every claimed platform |
| AC-04 | Invalid or hostile YAML, unknown fields, duplicates, invalid names, and collisions fail before mutation | Implemented | symlink-refusing 1 MiB bounded direct reads; pure-Rust pre-expansion rejection of tags, anchors, aliases, excessive depth, nodes, collections, and scalars; configuration tests; complete-registry route and non-routable project-identity collision tests; transactional registry tests; typed `configuration_invalid`, `configuration_collision`, and non-overridable `security_approval_blocked` diagnostics with exact claimants and change-deduplicated logging; automatic failures persist restart-restored structured snapshot events and remain visible through nonzero daemon status; explicit reconciliation returns the typed issues, records failure, and exits nonzero; blocked background scans retain and keep reconciling the last complete validated Engine plan without publishing partial desired state | None at repository-test level |
| AC-05 | Domains are exactly `{project}-{service}.stackctl.localhost` without repair | Implemented | identity and `composite_name_collision_fails_instead_of_receiving_a_fallback_domain` tests | None at repository-test level |
| AC-06 | Default domains resolve without hosts edits or a DNS daemon | Pending live evidence | setup and production startup typed `.localhost` resolver preflight before state mutation; actionable resolver/empty/non-loopback diagnostics that explicitly refuse hosts-file mutation; deliberate no-fallback policy preserving one deterministic suffix and wildcard certificate; host-dependency audit | Loopback-resolution acceptance artifact for every claimed platform |

## Gateway, TLS, and workload plane

| ID | Requirement | State | Authoritative evidence | Missing evidence or work |
|---|---|---|---|---|
| AC-07 | HTTPS is trusted after one-time setup | Pending live evidence | Stackctl CA/leaf tests, exact X.509 leaf-expiry inspection with explicit `certificate_expired` route health on failed replacement activation, real-file certificate bundle loading, symbolic-link-refusing private cross-process certificate transaction locking, interrupted-staging recovery, bidirectional partial OS-command rollback, transactional initial trust finalization, rollback-safe Debian managed-root refresh, atomic active-generation selection, validated typed Engine-only certificate activation independent of blocked watched-root scans, gateway-acknowledged bounded leaf-generation cleanup, zero-outage CA rotation rollback, and OS trust adapters | Install, renewal, rotation, removal, and browser trust records per claimed platform |
| AC-08 | No host Caddy or nginx is required | Implemented | pinned gateway container request; host-dependency audit | Live clean-host installation record |
| AC-09 | One managed gateway routes all projects | Implemented | gateway plane, directory-serialized and symbolic-link-refusing bootstrap publication, atomic full-snapshot, exact-domain health with explicit `gateway_route_drift`, route-aware browser opening, readiness, rollback, and port-conflict tests; native Linux amd64/arm64 CI publishes separate source-, host-, Engine-, and toolchain-attributed pinned-image protocol records; `docs/v8/evidence/gateway-protocol-macos-arm64.md` proves the pinned image's HTTP/1.1, HTTP/2, WebSocket, streaming, large-body, atomic reload, and restart contract | Link successful Linux CI records and produce equivalent raw records on macOS x86_64 |
| AC-10 | Application containers own no separate trusted CA | Implemented | gateway terminates TLS; app upstream plans are internal plain HTTP; dependency audit | Live container inspection artifact |
| AC-11 | Project runtimes and hooks execute in Linux containers | Pending live evidence | immutable application, invoking-user UID:GID application and worker execution, digest-pinned Composer/Node/Bun stages, project command, worker, daemon-timed scheduler exec, Reverb, and Engine exec paths; native Ubuntu x86_64/arm64 CI acceptance definitions build the derived runtime, mount project source, verify created-file ownership, execute PHP, Composer, npm, Bun, and a declared hook, and drive a pinned real Laravel skeleton from rejected fatal bootstrap through framework `/up`, strict CA-file HTTPS, Artisan, worker liveness, scheduler output, two-schema automatic restore, primary-only migration, restart-stable replay rejection, stable-CA reconciliation, daemon restart, and exact cleanup | Archive and link the release revision's Engine and Laravel acceptance artifacts |
| AC-12 | Declared PHP extensions work without host PHP | Partial | Public digest-locked FrankenPHP base; content-addressed local extension installation and verification; Engine resolution of every immutable input; exact worker image inheritance and scheduler execution inside the application container; native Ubuntu x86_64/arm64 derived-runtime build and in-application extension, Composer, Node, and Bun execution definition | Archive the release revision's Engine acceptance evidence |
| AC-13 | App containers publish no routine web ports and cannot directly reach other projects | Implemented | application and daemon plans place every workload on its deterministic project network; shared instances attach only to current consumers; native Ubuntu x86_64/arm64 CI acceptance creates two apps, a gateway, and one selectively attached shared endpoint, rejects host TCP bindings and cross-project/unauthorized probes, and proves gateway reachability to both apps | Archive and link the release revision's Engine acceptance artifacts |

## Shared services and state

| ID | Requirement | State | Authoritative evidence | Missing evidence or work |
|---|---|---|---|---|
| AC-14 | Compatible safe services share exact-fingerprint instances | Implemented for documented safe strategies | `docs/v8/services.md`; shared-instance resolver, bounded independent compatibility-instance convergence with serialized per-instance tenant provisioning, dedicated-routable strategy, service-specific plan tests, and native Ubuntu x86_64/arm64 CI acceptance definitions proving two compatible projects reuse one Redis, Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, RabbitMQ, MinIO, Mailpit, or stateless Gotenberg container across reconciliation, with persistent volumes where applicable, plus equivalent SQL Server acceptance on its published amd64 architecture | Archive the release revision's Redis, Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, SQL Server, RabbitMQ, MinIO, Mailpit, and Gotenberg Engine artifacts |
| AC-15 | Incompatible profiles split with an exact explanation | Implemented | compatibility fingerprint and shared resolver tests; native Ubuntu x86_64/arm64 real-YAML acceptance proving PostgreSQL 17 and 18 resolve to distinct fingerprints and owned containers carrying exact `postgresql/17` and `postgresql/18` compatibility labels | Archive the release revision's mixed-version Engine artifact |
| AC-16 | Logical resources and credentials converge idempotently | Partial | PostgreSQL, MySQL/MariaDB, MongoDB, Redis/Valkey, object-store, RabbitMQ, Mailpit, SQL Server, a common bounded transient startup strategy with service-specific bootstrap-authenticated Redis/Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, and RabbitMQ probes before logical provisioning, native Ubuntu x86_64/arm64 Redis, Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, RabbitMQ, MinIO, Mailpit, and Gotenberg CI acceptance definitions plus amd64 SQL Server acceptance proving stable replayed credentials, distinct logical namespaces or attribution, successful tenant writes or conversions, authentication or cross-project denial where applicable, and unchanged-container reuse, dedicated Dragonfly authenticated readiness, dedicated Garage authenticated bucket verification, dedicated LocalStack bucket provisioning, dedicated Memcached protocol readiness, native Ubuntu x86_64/arm64 CI acceptance driving real YAML through dedicated Dragonfly, Memcached, Meilisearch, Typesense, Garage, LocalStack, RustFS, Soketi, Elasticsearch, and OpenSearch planning, private-network startup, authenticated or exact-protocol readiness, incorrect credential rejection, retained-volume handling, deterministic bucket removal detection and repair, owned-container replacement recovery, container-health confirmation, exact deterministic route verification, and exact cleanup, pinned-client authenticated Elasticsearch, OpenSearch, Meilisearch, and Typesense readiness with distinct HTTP 401/403 `authentication_failed` evidence, isolated `service_not_ready` retry state, and project-scoped `logical_resource_drift` that preserves the shared instance and continues later tenants without disconnecting a healthy Engine; real-directory-locked shared and project configuration and credential publication | Archive the release revision's Redis, Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, SQL Server, RabbitMQ, MinIO, Mailpit, and Gotenberg Engine artifacts |
| AC-17 | Credentials remain stable across daemon and Engine restarts | Implemented | SQLite insert-if-absent, private database/WAL/SHM permissions, symbolic-link refusal for state and managed secret files, directory-serialized secret publication, redaction, restart, shared bootstrap credential tests, and native Ubuntu x86_64/arm64 clean-room Engine-restart acceptance retaining both restored database identities and contents | Archive the release revision's Linux restart artifact; Docker Desktop evidence remains external |
| AC-18 | Persistent resources are never pruned implicitly | Partial | retention policy tests; orphan stop behavior; exact seven-day disposable container and ownership-proven unreferenced build-image GC tests; common token-bound PostgreSQL, MySQL/MariaDB, MongoDB, SQL Server, Redis/Valkey, RabbitMQ, and MinIO prune execution with immediate stored-artifact reverification; resource-directory-serialized, symbolic-link-refusing recovery-point publication; RabbitMQ non-durable queues, non-persistent messages, quorum queues, and streams are explicitly unsupported and fail closed before prune; keep-data-default and terminal-marker-gated delete-data uninstall, typed secret-free whole-installation plan/confirm/status IPC, recovery-bound project-volume confirmation and exact Engine authorization with unlisted observed-volume refusal, idle artifact-reverified confirmed freeze, restart-safe one-at-a-time logical prune scheduling, durable deleting/deleted reconciliation freeze, daemon-owned dependency-ordered exact Engine cleanup including derived images, logical-and-operation-empty terminal deletion invariant, native Ubuntu x86_64/arm64 Redis and Valkey acceptance streaming verified tenant-prefix backups and staged exact-prefix restores while removing post-backup keys and preserving sibling namespaces, PostgreSQL acceptance streaming a verified custom dump into an isolated retained target while preserving the later-mutated rollback source and sibling database, MySQL and MariaDB acceptance streaming verified logical dumps into isolated retained targets while preserving later-mutated rollback sources and sibling schemas, MongoDB acceptance streaming a verified archive into an isolated retained target while preserving the later-mutated rollback source and sibling database, SQL Server acceptance streaming a verified native backup into an isolated retained target while preserving the later-mutated rollback source and sibling database, RabbitMQ acceptance streaming verified topology and persistent classic-queue messages through an in-place vhost restore while preserving the sibling vhost, MinIO acceptance streaming a verified current-object archive through the exact owned volume and restoring the selected bucket while removing post-backup objects and preserving the sibling bucket, plus CI acceptance definitions proving an unauthorized persistent project volume is retained before exact-authorized deletion and a durable recovery-bound deletion plan requires its confirmation token before deleting the exact volume and committing terminal state | Archive the release revision's Engine acceptance artifacts; physical-host uninstall keep-data/delete-data acceptance remains incomplete |

## Engine, daemon recovery, and lifecycle

| ID | Requirement | State | Authoritative evidence | Missing evidence or work |
|---|---|---|---|---|
| AC-19 | Normal operation uses typed Engine APIs, not CLI parsing | Implemented | capability traits, Bollard adapter, v8 host-dependency audit, direct strict-v8 dispatch, and native Ubuntu x86_64/arm64 negotiation plus real owned application/network lifecycle and managed-inventory acceptance definitions with raw artifact publication | Archive and link the release revision's Engine acceptance artifacts |
| AC-20 | Engine unavailability and restart recover automatically | Pending live evidence | production managed-event subscription, cursor-based bounded reconnect, bounded connection backoff, gateway discovery/readiness/admin-reload transport adapter invalidation with due-plan retention, explicit `engine_unavailable` project and daemon status from startup through connection loss, container `restarting` project health, health invalidation, event scheduling, rescan recovery tests, and native Ubuntu x86_64/arm64 CI acceptance that restarts Docker while the daemon remains running and requires the unchanged application, route, and restored data to recover | Archive the release revision's Linux Engine-restart artifacts; Docker Desktop unavailable/start/restart and sleep/wake records remain external |
| AC-21 | Daemon restart, login, reboot, service crash, sleep, and wake recover | Partial | atomic SIGINT/SIGTERM loop exit, active-operation drain without claiming queued work, normal socket/lease teardown, queue/state restore, directory-serialized stable interrupted-backup staging recovery, idempotent reconciliation, bounded persistent fatal-startup diagnostics, explicit 30-second launchd/systemd fatal-restart pacing, restart, and crash tests | Login, reboot, sleep/wake, and service-crash platform artifacts |
| AC-22 | Removing/restoring config follows retention rules | Partial | atomic orphaning, transactional atomic-directory-rename handling that retires the stale route owner without implicit adoption, credential disablement, typed daemon-authoritative table/JSON visibility for orphaned and retained project resources after registry removal, adoption, daemon-boundary real-YAML acceptance proving removal, rediscovery, exact adoption, and atomic directory rename preserve stable retained resource, logical-data, credential, and environment identity without implicit reactivation, daemon-reachable shared-access strategy with ownership-proven RabbitMQ, Redis/Valkey, MySQL/MariaDB, and MongoDB user deletion plus PostgreSQL `NOLOGIN`, SQL Server login disablement, and MinIO identity disablement before service idling, PostgreSQL, MySQL/MariaDB, MongoDB, and SQL Server backup and crash-replayable prune, daemon-owned Redis/Valkey prefix backup, prune, and safety-backed in-place restore, recovery-bound RabbitMQ prune plus broker-wide quiesced tenant-scoped durable persistent classic-queue message backup, explicit fail-closed rejection of non-durable queues, non-persistent messages, quorum queues, and streams, credential-free topology, tar-subtree validation, and network-isolated safety-backed restore, recovery-bound MinIO current-object backup, exact tenant prune, and safety-backed in-place restore for unversioned buckets, ownership-reverified quiesced backup, safety-backed empty-target restore, recovery-bound installation deletion and prepared service-request replay for dedicated project volumes, typed isolated `destructive_replacement_required` health for retained project-volume identity drift, disposable GC, reversible PostgreSQL, MySQL/MariaDB, MongoDB, and SQL Server restore/cutover/confirm/rollback, explicit keep-data/delete-data paths with directory-serialized terminal-marker publication and failed-operation retry tests, native Ubuntu x86_64/arm64 Redis and Valkey acceptance driving real watched YAML through Engine publication, verified prefix backup and staged restore, removal, durable orphan and credential disablement, access revocation, retained data, sibling continuity, restoration, exact adoption, stable-credential reactivation, and same-container reuse, plus equivalent PostgreSQL, MySQL, MariaDB, MongoDB, RabbitMQ, MinIO, confirmed installation-volume deletion, and amd64 SQL Server lifecycle acceptance | Physical-host uninstall keep-data/delete-data acceptance; archive the release revision's lifecycle Engine artifacts |
| AC-23 | V8 enforces a clean-install major-version boundary | Implemented | v8-only CLI parser and dispatch, exact `.stackctl.yaml` discovery with no alternate configuration lookup, atomic current-schema initialization with explicit non-empty-state rejection, and no config conversion command | Clean-host installation acceptance |

## Supply chain, platforms, and efficiency

| ID | Requirement | State | Authoritative evidence | Missing evidence or work |
|---|---|---|---|---|
| AC-24 | macOS and Linux claims have platform evidence | Pending live evidence | `docs/v8/platform-support.md`; release-build and full-suite Unix architecture CI matrix; platform-gated trust adapters with a source-policy regression; native Ubuntu production Engine adapter API acceptance; top-level unsupported-host compile boundary with no production compatibility fallbacks | Complete successful exact-revision CI and live macOS and Linux records |
| AC-25 | Built-in images and release binaries are immutable and verified with no mutable installer pipelines | Implemented at repository-test level | digest validation, directory-serialized crash-recoverable artifact-lock publication, manifest-pinned public PHP image definition, commit-pinned CI and release actions with enforcement audits, native Linux and macOS x86_64/arm64 locked binary builds, exact architecture assertions, tag-to-package version enforcement, SPDX SBOM generation, GitHub Sigstore provenance and SBOM attestations, checksum verification, retained raw evidence, safe immutable tool-image references, offline content-addressed project builds with one final complete ownership-label instruction that avoids partially managed BuildKit intermediates, runtime fingerprint, and dependency audit tests | Run the release workflow for the exact release revision, then archive and link its verified evidence bundle |
| AC-26 | Host dependency audit proves removed executables absent | Implemented | whole-v8-source `scripts/audit-v8-host-dependencies.sh`, removed-tree assertions, and required CI job | Clean-host runtime acceptance |
| AC-27 | Forty-project benchmark substantially improves idle usage | Pending live evidence | `scripts/benchmark-v8.sh`; shared run and collector identity; nonempty independently inventoried Engine-idle and per-project baseline capture; typed ownership-scoped daemon samples gated on current desired-state convergence with exact registered ownership, service implementation and major-version profiles, fingerprint enforcement, and atomic publication; release-mode synthetic 1/10/40-project discovery percentile regression gate in `scripts/benchmark-v8-discovery.sh`; `docs/v8/benchmarks.md` | Run all resource baseline and v8 scenarios, then publish raw records plus threshold comparison; archive the discovery CI record |
| AC-28 | Relevant unit, integration, recovery, chaos, platform, build, and lint checks pass | Partial | 884 passing local tests, twenty-three explicit CI-owned live Engine acceptances, and one explicit release-mode discovery benchmark; format, all-target Clippy policy, build, host-dependency audit, immutable-workflow-action audit, guarded durable cancellation of queued daemon operations, bounded independent workload and compatibility-instance reconciliation, pre-shared container observation reuse for interrupted ephemeral cleanup, pass-wide post-shared container observation reuse across orphan access revocation, cleanup, applications, workers, dedicated services, and transient provisioning jobs, bounded dedicated project-service convergence after volume eligibility, bounded ordered service-readiness and provisioning jobs with serialized retry state, pass-wide volume observation reuse across shared and dedicated retained volumes, complete retained-volume ownership preflight before bounded independent mutation, pass-scoped shared network observations, equal-runtime materialization deduplication, common authenticated Redis/Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, and RabbitMQ cold-start readiness, authenticated PostgreSQL, MySQL/MariaDB, MongoDB, and SQL Server retained-migration-target readiness, daemon-boundary watched-YAML removal/restoration/rename/adoption lifecycle, recovery-authorized confirmed installation-volume deletion, immutable Linux PHP/Composer/Node/Bun derived-runtime build and in-application command/hook execution, live incompatible PostgreSQL 17/18 instance splitting with exact profile labels, dedicated Dragonfly authentication, Memcached protocol readiness, authenticated Meilisearch, Typesense, Elasticsearch, and OpenSearch readiness with incorrect-credential rejection, Garage, LocalStack, and RustFS bucket provisioning and drift repair, Elasticsearch and OpenSearch retained-volume container replacement recovery, and Soketi container-health and route verification, live watched-YAML Redis/Valkey verified backup, staged exact-prefix restore, removal, orphaning, Engine access revocation, restoration, exact adoption, and stable access reactivation, live isolated-target PostgreSQL, MySQL, MariaDB, MongoDB, and SQL Server backup and restore with preserved rollback sources and sibling logical resources, live RabbitMQ verified topology and persistent classic-queue backup plus in-place selected-vhost restore with sibling preservation, equivalent live PostgreSQL/MySQL/MariaDB/MongoDB/RabbitMQ/MinIO/SQL Server project removal and restoration, and live shared Redis, Valkey, PostgreSQL, MySQL, MariaDB, MongoDB, SQL Server, RabbitMQ, MinIO, Mailpit, and Gotenberg protocol, isolation, or attribution at this snapshot; release-build, full-suite Unix architecture CI, and architecture-specific raw Engine acceptance publication definitions | CI artifacts and external platform, recovery, image publication, and benchmark records assigned by `external-verification.md` |

## External release-evidence backlog

The following items block only the corresponding v8 release and platform
support claims. They are not part of the per-change local verification cycle
and MUST NOT pause implementation or checkpoint commits:

| Evidence | Execution owner |
| --- | --- |
| macOS install, login, reboot, sleep/wake, Keychain trust, and Docker Desktop recovery | Attended physical Mac or an equivalent managed hardware harness |
| Linux install, systemd-user login, reboot, suspend/wake, and Engine recovery | Persistent Linux host; automate where the host lifecycle permits |
| Persistent deletion and uninstall keep-data/delete-data acceptance | CI for container data semantics; physical hosts for service-manager and trust-store cleanup |
| Release binary SBOM, provenance, signature, and architecture proof | Release workflow for the exact release revision |
| Gateway, runtime, extension, and shared-service protocol acceptance | CI where the behavior is container- or protocol-owned; physical hosts only for OS integration |
| Forty-project baseline/v8 benchmark | Unattended script on a controlled dedicated benchmark host, followed by result review |

Every blocker must link raw, reproducible evidence here before its row changes
to `Complete`. A passing compile, unit test, interface, plan, or document cannot
change a live-evidence row by itself.
